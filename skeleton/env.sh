export DATABASE=database.db
export PORT_HTTP=80
# Remove this line to serve HTTP-only
export PORT_HTTPS=443
# Host from which the site is served
export HOST=
# Salt used to hash passwords; should be unique
export SALT=
# Secret used to sign authentication cookies
export SECRET=
# Path to the public HTTPS certificate
export CERTFILE=
# Path to the private key for the above HTTPS certificate
export KEYFILE=
# Path to deploy to on the webserver
export DIR=
